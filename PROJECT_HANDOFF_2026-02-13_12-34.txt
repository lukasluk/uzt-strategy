PROJECT HANDOFF - UZT / digistrategija.lt
Generated: 2026-02-13 12:34
Repo: https://github.com/lukasluk/uzt-strategy
Branch: master

==================================================
1) CURRENT SNAPSHOT (2026-02-13)
==================================================
Production domain:
- https://www.digistrategija.lt
- https://digistrategija.lt

Most recent verified production deployment:
- Date/time: 2026-02-23 22:02 UTC
- Result: Deployment completed safely
- DB backup:
  /srv/uzt-backups/database/uzt_strategy_20260223T220229Z.dump
- Release backup:
  /srv/uzt-backups/releases/20260223T220229Z
- Health endpoint:
  /api/v1/health => {"ok":true,"version":"v1"}

Latest deployed commit:
- 8cb9dea - Fix mojibake symbols and UTF-8 text across app and landing

Latest applied source update in this handoff cycle (to be deployed if not yet):
- Institution admin users flow switched from direct password set to one-time URL flow.
- Invite generation now returns invite URL (not only raw token), with expiry metadata.
- Added invite activation page:
  - prototype/accept-invite.html
  - prototype/accept-invite.js
- Added backend endpoint for institution-admin reset-link generation:
  - POST /api/v1/admin/users/:userId/password-reset-link
- Added backend endpoint to validate invite token before acceptance:
  - GET /api/v1/invites/token-info
- Mobile UI refinements for phone screens:
  - tuned proportions at <=640px and <=480px breakpoints
  - compacted header, toolbar, cards, buttons, and form fields
  - improved mobile map controls and overlay layout
  - improved login modal sizing and spacing on small screens
- Left navigation quick-add expansion for steps:
  - active "Gairės" menu item can expand and show "Pridėti naują gairę"
  - active "Iniciatyvos" menu item can expand and show "Pridėti naują iniciatyvą"
  - quick-add action smooth-scrolls to the matching add section and focuses first input
- Meta-admin invite flow now emphasizes one-time invite URL:
  - meta-admin invite API returns inviteUrl + expiresAt
  - meta-admin UI shows latest one-time invite URL block with copy/open actions
  - token fallback remains only for compatibility if inviteUrl is missing
- Strategy map parent guideline cards were further emphasized:
  - stronger parent card border/accent/shadow
  - added clear "Tėvinė" badge in map nodes for parent guidelines
- Strategy map parent emphasis refinement:
  - removed "Tėvinė" badge from map parent cards (cleaner visual)
  - made parent card border/accent stronger for clearer distinction
  - highlighted institution -> parent guideline lines with brighter, thicker blue
- Strategy map parent scale + consistency update:
  - parent guideline card border thickness now matches institution -> parent line thickness
  - parent guideline cards increased by ~20% (node size)
  - parent card internal text and key UI labels scaled up for readability
- Strategy map institution emphasis alignment:
  - institution card border thickness aligned to parent-card/root-link accent thickness
  - institution active pulse ring size increased for stronger map focus signal

==================================================
2) DEPLOYMENT STATUS - STABLE
==================================================
Deployment process is stable and repeatable.

In place:
- GitHub Actions deployment workflow:
  .github/workflows/deploy.yml
- Safe server deploy script:
  deploy/deploy.sh
  Features:
  - lock file protection
  - env validation
  - DB backup before deploy
  - release snapshot backup
  - post-deploy health check

Latest behavior observed:
- nginx configuration test passes during deploy.
- API service restarts successfully.
- Health check typically passes on attempt 2/20.

==================================================
3) SUDO CONFIGURATION STATUS
==================================================
Deployment is executed in a sudo-capable environment.

Confirmed:
- Server operations were run from root shell:
  root@srv1345585
- deploy script uses sudo-requiring operations for:
  - systemd restart/status
  - nginx config test/reload

Conclusion:
- sudo path is configured and working for current deploy routine.

==================================================
4) FRONTEND STATUS
==================================================
Frontend remains static + vanilla JS:
- prototype/index.html
- prototype/app.js
- prototype/app-map.js
- prototype/admin.html
- prototype/admin.js
- prototype/meta-admin.html
- prototype/meta-admin.js
- prototype/reset-password.html
- prototype/reset-password.js
- prototype/accept-invite.html (new)
- prototype/accept-invite.js (new)
- prototype/styles.css
- prototype/i18n.js

Current focus:
- Institution admin user management UX cleanup:
  - remove inline direct password set field from participant cards
  - generate one-time reset URLs per selected participant
  - show latest generated URL + copy button in admin UI
  - regrouped institution-admin participants UI so each user and related actions are in a clear card block
  - regrouped meta-admin user actions/memberships to prevent button overlap and improve readability
  - adjusted top-left DS logo: reduced size by ~30% and added subtle glass/emboss styling
  - centered DS mark inside fixed logo square, darkened square background, and increased emboss depth
  - removed redundant "Iniciatyva" tag from initiative cards in the Initiatives view
  - reduced top-left DS icon tile by ~40% for a more compact header footprint
  - added initiative page matrix panel (Guideline -> Initiatives) with scrollable table, alternating row colors, and explicit highlight for unassigned guidelines
  - replaced guideline assignment UX from Ctrl/Cmd multiselect to checkbox lists in initiatives flow (member view and institution admin add/edit forms)
  - widened initiative "Trumpas paaiškinimas" input area to full form width
  - stretched "Gairiu ir iniciatyvu susiejimas" panel to match full height of the "Nauja iniciatyva" block
  - switched top-left DS tile to bright yellow styling
  - updated favicon to matching yellow DS theme and bumped favicon cache version across all prototype HTML pages

==================================================
5) BACKEND STATUS
==================================================
Backend remains modularized v1 API.

Key route modules:
- backend/src/authRoutes.js
- backend/src/adminRoutes.js
- backend/src/metaAdminRoutes.js
- backend/src/publicRoutes.js
- backend/src/memberRoutes.js

Password reset token logic:
- backend/src/passwordResetService.js

Health endpoint:
- /api/v1/health

==================================================
6) KNOWN SECURITY NOTE (ACTIVE)
==================================================
Runtime warning still present in logs:
- META_ADMIN_PASSWORD_HASH is not set; legacy plaintext meta-admin password flow is active.

Recommended hardening:
- Set META_ADMIN_PASSWORD_HASH
- Disable legacy fallback:
  ALLOW_LEGACY_META_ADMIN_PASSWORD=0

Helper:
- node backend/scripts/generate-meta-admin-hash.js "<strong-password>"

==================================================
7) MANDATORY HANDOFF UPDATE RULE
==================================================
This file is mandatory operational context.

It MUST be updated after every:
1) behavior-changing commit
2) production deployment

Required updates each time:
- latest commit hash/message
- deployment date/time
- health-check output
- backup paths
- warnings/errors
- next-step recommendation

Rule for filename:
- After each update, rename file to newest timestamp:
  PROJECT_HANDOFF_YYYY-MM-DD_HH-mm.txt

==================================================
8) STANDARD PUBLISH + DEPLOY RUNBOOK (MANUAL)
==================================================
# Local machine (publish to GitHub)
cd <local-repo>/uzt-strategy
git status --short
git push origin master
git rev-parse --short HEAD

# Server (deploy published commit)
cd /srv/uzt-strategy-src
sudo git fetch origin
sudo git reset --hard origin/master
git log --oneline -n 3
sudo bash /srv/uzt-strategy-src/deploy/deploy.sh
sudo systemctl status uzt-strategy-api --no-pager
curl -fsS http://127.0.0.1:3000/api/v1/health
curl -fsS https://digistrategy.eu/api/v1/health

# UTF-8/mojibake regression check (should print no matches)
curl -fsS https://digistrategy.eu/app.js | rg "GairÄ|StrategijÅ|â€ž|âš™|âœ¦|âŒ"

==================================================
9) NEXT RECOMMENDED STEP
==================================================
After deploying this update:
- Verify institution admin UI no longer shows:
  - "Naujas slaptažodis (min. 8)" inline field
  - direct password update button
- Verify participant card shows:
  - "Slaptažodžio keitimo nuoroda" action
- Verify invite flow:
  - generated invite URL opens accept-invite page
  - accept page creates password + display name + email validation

==================================================
10) CRITICAL PRODUCTION-SAFETY NOTE (2026-02-22)
==================================================
Platform is actively used in production. Any deployment that touches strategy-creation logic
MUST follow extra caution:

- Always run deploy/deploy.sh (never partial manual overwrite).
- Confirm DB backup path printed by deploy script before continuing.
- Run health checks immediately after deploy:
  - curl -fsS http://127.0.0.1:3000/api/v1/health
  - curl -fsS https://digistrategy.eu/api/v1/health
- Validate one read path and one write path in production UI/API.
- If migration/health fails, stop and rollback via release backup.

New feature safety requirement:
- AI-assisted strategy generation MUST create only new strategy/cycle data and must not mutate existing data.
- Keep strict upload limits and input validation enabled.
- Upload parser upgraded to multer@2.x for safer dependency baseline.
- Default AI model for PDF strategy generation set to gpt-5-mini (override via AI_STRATEGY_MODEL).

==================================================
11) MAP REFACTOR BASELINE (2026-03-02)
==================================================
- Added `prototype/map-shared.js` for shared map constants + helper functions.
- Updated script load order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so `map-shared.js` loads before `app-map.js`.
- Removed duplicated constant/helper declarations from `prototype/app-map.js`.
- Added architecture notes: `docs/map-architecture.md`.
- Behavior target of this step: no functional change, only structure/maintainability.

==================================================
12) MAP REFACTOR PHASE 2 (2026-03-02)
==================================================
- Added `prototype/map-geometry.js`:
  - edge geometry/path helpers
  - viewport fit/transform helpers
  - map edge refresh helpers
- Added `prototype/map-interactions.js`:
  - focus/hover interaction state for initiatives layer
  - drag/pan/zoom bindings
  - node position persistence
- Updated script order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so new files load before `app-map.js`.
- Removed moved function blocks from `prototype/app-map.js`.
- Updated architecture document to reflect new module boundaries.
- Behavior target: no functional change, structure only.

==================================================
13) SECURITY DEPENDENCY UPDATE (2026-03-02)
==================================================
- Context:
  - Production deploy reported:
    - `multer <=2.0.2` high severity DoS advisories
    - GHSA-xf7r-hgr6-v32p
    - GHSA-v52c-386h-88mc
- Action:
  - Updated backend dependency:
    - `multer` from `^2.0.2` -> `^2.1.0`
  - Files updated:
    - `backend/package.json`
    - `backend/package-lock.json`
- Verification:
  - `npm ls multer` => `multer@2.1.0`
  - `npm audit` => `found 0 vulnerabilities`
- Deployment evidence from production:
  - Migration completed safely
  - Health checks passed (`/api/v1/health` local + public)
  - Backup paths:
    - `/srv/uzt-backups/database/uzt_strategy_20260302T185221Z.dump`
    - `/srv/uzt-backups/releases/20260302T185221Z`

==================================================
14) MAP REFACTOR PHASE 3 (2026-03-02)
==================================================
- Added `prototype/map-layout.js`:
  - moved graph-building/layout functions out of `app-map.js`
  - includes:
    - `estimateGuidelineNodeHeight`
    - `estimateInitiativeNodeHeight`
    - `layoutStrategyMap`
    - `layoutStrategicLinksMap`
- Updated script load order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so `map-layout.js` loads before `app-map.js`.
- Reduced `prototype/app-map.js` responsibility to rendering/composition:
  - keeps `notifyMapError`
  - keeps `renderMapView`
  - adds `ensureMapRuntimeDependencies` guard for clearer module-load failures
- Updated architecture notes:
  - `docs/map-architecture.md`
- Behavior target: no functional change, structure/maintainability only.

==================================================
15) MAP REFACTOR PHASE 4 (2026-03-02)
==================================================
- Refactored `prototype/app-map.js` render orchestration into focused helpers:
  - `renderMapLoadingState`
  - `renderMapErrorState`
  - `renderMapEmptyState`
  - `renderMapInstitutionPromptState`
  - `resolveActiveMapLayer`
  - `renderStrategicLinksPendingState`
  - `resolveMapGraphForLayer`
  - `buildMapCommentItems`
  - `setMapLayerAndRender`
  - `bindMapLayerButtons`
- `renderMapView` now acts as coordinator for:
  - pre-render state checks
  - layer/graph resolution
  - markup rendering
  - post-render bindings
- Updated architecture notes (`docs/map-architecture.md`) to reflect new helper boundaries.
- Behavior target: no functional change, only readability/maintainability improvements.

==================================================
16) MAP REFACTOR PHASE 5 (2026-03-02)
==================================================
- Added `prototype/map-render.js` for render-markup builders:
  - `buildMapHeaderMarkup`
  - `buildMapToolbarMarkup`
  - `buildGuidelineEdgeMarkup`
  - `buildStrategyGuidelineEdgeMarkup`
  - `buildStrategicEdgeMarkup`
  - `buildInitiativeEdgeMarkup`
  - `buildMapViewShellMarkup`
- Updated script order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so `map-render.js` loads before `app-map.js`.
- Updated `prototype/app-map.js` to use new builders for:
  - header/toolbar markup
  - edge SVG markup
  - map shell + modal markup assembly
- Extended map runtime dependency guard with `map-render.js` helper checks.
- Updated architecture notes (`docs/map-architecture.md`) with new module boundaries.
- Behavior target: no functional change, structure/maintainability only.

==================================================
17) MAP REFACTOR PHASE 6 (2026-03-03)
==================================================
- Continued extraction into `prototype/map-render.js`:
  - added node-card builders:
    - `buildInstitutionNodeMarkup`
    - `buildGuidelineNodeMarkup`
    - `buildInitiativeNodeMarkup`
    - `buildNodeMarkup`
- Updated `prototype/app-map.js`:
  - replaced inline `nodeMarkup` block with `buildNodeMarkup({ graph, activeLayer, editable })`
  - extended runtime dependency guard with `buildNodeMarkup`
- Updated architecture notes (`docs/map-architecture.md`) to reflect node builder extraction.
- Behavior target: no functional change, structure/maintainability only.

==================================================
18) MAP REFACTOR PHASE 7 (2026-03-03)
==================================================
- Continued extraction of map interaction wiring into `prototype/map-render.js`:
  - moved map comment modal helpers from `app-map.js`:
    - `buildMapCommentItems`
    - `bindMapCommentModalInteractions`
- Updated `prototype/app-map.js`:
  - replaced inline modal setup/listener block with:
    - `bindMapCommentModalInteractions({ stepView: elements.stepView, graph })`
  - updated runtime dependency guard to require `bindMapCommentModalInteractions`
- Updated architecture notes (`docs/map-architecture.md`) to reflect modal wiring extraction.
- Behavior target: no functional change, structure/maintainability only.

==================================================
19) MAP REFACTOR PHASE 8 (2026-03-03)
==================================================
- Continued extraction of map interaction wiring into `prototype/map-render.js`:
  - added:
    - `bindMapStrategyNavigationInteractions`
    - `bindMapViewportControlInteractions`
- Updated `prototype/app-map.js`:
  - replaced inline handlers with:
    - `bindMapStrategyNavigationInteractions({ ... })`
    - `bindMapViewportControlInteractions({ ... })`
  - added runtime dependency checks for both new helpers.
- Updated architecture notes (`docs/map-architecture.md`) to reflect extracted handlers.
- Behavior target: no functional change, structure/maintainability only.

==================================================
20) MAP REFACTOR PHASE 9 (2026-03-03)
==================================================
- Added new module: `prototype/map-view-state.js`:
  - moved map state-card helpers:
    - `renderMapLoadingState`
    - `renderMapErrorState`
    - `renderMapEmptyState`
    - `renderMapInstitutionPromptState`
    - `renderStrategicLinksPendingState`
  - moved layer-toggle helpers:
    - `setMapLayerAndRender`
    - `bindMapLayerButtons`
- Updated script load order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so `map-view-state.js` loads before `app-map.js`.
- Updated `prototype/app-map.js`:
  - removed moved helper implementations
  - extended runtime dependency checks for moved helpers.
- Updated architecture notes (`docs/map-architecture.md`) with new module boundaries and next steps.
- Behavior target: no functional change, structure/maintainability only.

==================================================
21) MAP REFACTOR PHASE 10 (2026-03-03)
==================================================
- Added new module: `prototype/map-view-resolve.js`:
  - moved:
    - `resolveActiveMapLayer`
    - `resolveMapGraphForLayer`
- Updated script load order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so `map-view-resolve.js` loads before `app-map.js`.
- Updated `prototype/app-map.js`:
  - removed moved resolver implementations
  - extended runtime dependency checks for resolver helpers.
- Updated architecture notes (`docs/map-architecture.md`) to include `map-view-resolve.js` responsibilities.
- Behavior target: no functional change, structure/maintainability only.

==================================================
22) MAP REFACTOR PHASE 11 (2026-03-03)
==================================================
- Added new module: `prototype/map-view-compose.js`:
  - added `buildMapViewRenderPayload({ graph, activeLayer, hasInitiativeNodes, editable })`
  - moved render-body assembly (map shell argument preparation) out of `app-map.js`.
- Updated script load order in:
  - `prototype/index.html`
  - `prototype/embed/strategy-map/index.html`
  so `map-view-compose.js` loads before `app-map.js`.
- Updated `prototype/app-map.js`:
  - replaced inline render payload preparation with:
    - `const mapRenderPayload = buildMapViewRenderPayload(...)`
    - `buildMapViewShellMarkup(mapRenderPayload)`
  - extended runtime dependency checks with `buildMapViewRenderPayload`.
- Updated architecture notes (`docs/map-architecture.md`) with `map-view-compose.js` responsibilities.
- Behavior target: no functional change, structure/maintainability only.

==================================================
23) MAP CARD WIDTH + WORD-WRAP FIX (2026-03-03)
==================================================
- Goal:
  - Prevent broken words inside floor-map cards (example: `nacionalinius` split as `nacionaliniu` + `s`).
  - Make card width adapt to the longest word content.
- Updated `prototype/map-layout.js`:
  - added dynamic width helpers:
    - `longestWordLength`
    - `estimateCardWidthFromLongestWord`
    - `estimateInstitutionNodeWidth`
    - `estimateGuidelineNodeWidth`
    - `estimateInitiativeNodeWidth`
  - applied dynamic width in both layouts:
    - standard strategy map
    - strategic-links map slices
  - card width now grows from base width when longest word requires more space.
- Updated `prototype/styles.css`:
  - disabled forced word splitting in map-card typography for guidelines/initiatives/strategic links:
    - removed `overflow-wrap:anywhere` / `word-break:break-word` behavior in map-card contexts
    - set `word-break: normal`, `overflow-wrap: normal`, `hyphens: none`
  - added `.map-node-head h4 { min-width: 0; }` to avoid flex-layout clipping edge cases.
- Expected result:
  - words are wrapped only at natural break points (spaces), not split mid-word
  - cards become wider when a long word appears.

==================================================
24) MAP CARD WIDTH TUNING (2026-03-03)
==================================================
- Follow-up adjustment after visual review:
  - some cards became wider than necessary.
- Updated `prototype/map-layout.js` width estimation:
  - reduced letter-width factor used for longest-word estimation.
  - reduced reserve/padding component in width formula.
  - added explicit width caps:
    - institution cards max width
    - guideline cards max width
    - initiative cards max width
  - in standard map layer guideline width no longer includes strategy title in width calculation
    (kept in strategic-links context where label footprint is larger).
- Expected result:
  - no mid-word splitting remains
  - cards are still adaptive but no longer over-expanded.

==================================================
25) MAP CARD UX CLEANUP (2026-03-03)
==================================================
- Requested UX updates implemented:
  - Move map card comment button to bottom-right corner.
  - Hide empty-vote text (`No votes yet`) on map cards.
  - Remove initiative card line `Initiative · Linked guidelines: N` from floor map.
- Updated `prototype/map-render.js`:
  - guideline/initiative card markup changed so comment button is rendered after content
    (for absolute bottom-right positioning in CSS).
  - vote squares are rendered only when score > 0.
  - removed initiative linked-guidelines subtitle row from initiative card.
  - normalized map comment button label text using `mapLang(...)`.
- Updated `prototype/styles.css`:
  - `.guideline-node` and `.initiative-node` now reserve bottom space for absolute comment button.
  - comment button anchored bottom-right inside guideline/initiative cards.
  - `map-node-head` aligned for title-only row.
  - parent guideline variant padding adjusted to preserve reserved button space.
- Expected result:
  - title row has full width for text
  - comment action remains visible, but no longer squeezes title width
  - cleaner initiative cards and no empty-vote noise.

==================================================
26) MAP WIDTH HOTFIX AFTER DEPLOY REGRESSION (2026-03-03)
==================================================
- Issue observed after latest deploy:
  - floor map looked visually broken/scaled down with disconnected-looking layout.
- Root cause:
  - dynamic width estimation still used non-visible text fields for map cards
    (especially long institution names in standard guideline cards),
    which inflated node widths and expanded fit bounds.
- Updated `prototype/map-layout.js`:
  - guideline width now uses owner label matching actual UI context:
    - standard map layer: `institution.slug + relationType`
    - strategic-links layer: `institution.name / strategy.title`
  - initiative width now uses only initiative title (linked-guidelines line was removed from card UI).
  - reduced initiative width reserve/max to keep map bounds tighter.
- Expected result:
  - map auto-fit no longer zooms out excessively
  - card sizing better matches visible content and layout remains stable.

==================================================
27) MAP POSITIONING REGRESSION HOTFIX (2026-03-03)
==================================================
- Issue observed in production screenshot:
  - guideline cards visually disconnected from edges
  - map composition appeared broken despite edge updates.
- Root cause:
  - `.guideline-node` and `.initiative-node` were set to `position: relative` in CSS,
    overriding inherited `position: absolute` from `.strategy-map-node`.
  - this changed card positioning model and broke coordinate alignment with SVG edges.
- Fix:
  - removed `position: relative` overrides from:
    - `.guideline-node`
    - `.initiative-node`
  - kept bottom-right comment button behavior via absolute button positioning inside
    absolutely positioned card container.
- Expected result:
  - cards return to coordinate-based absolute placement
  - edges reconnect correctly to card anchors.

END OF HANDOFF









==================================================
28) GUIDELINE CARD METADATA CLEANUP (2026-03-03)
==================================================
- Updated guideline card content to reduce visual noise and match requested UI:
  - removed child/parent relation label from guideline subtitle in standard layers
  - removed `Links: N` chip on guideline cards
  - kept strategic-links focus chip behavior unchanged in strategic-links layer
- Updated parent guideline styling so comment button size now matches child cards:
  - removed parent-only comment button/icon/count size overrides
- Files changed:
  - `prototype/map-render.js`
  - `prototype/styles.css`
- Expected result:
  - cleaner guideline cards with consistent metadata presentation
  - consistent comment button sizing between parent and child cards.
